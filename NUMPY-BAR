import matplotlib.pyplot as plt
import numpy as np
overs=np.arange(1,11)
scores=[9,2,4,7,8,13,6,7,3,5]
plt.bar(overs,scores)
plt.title('INDIA SCORE OVER WISE with Linear Regression Prediction')
plt.xlabel('OVERS FROM 1 TO 11')
plt.ylabel('SCORE RUNS BASED ON OVERS')
plt.xticks(overs)
plt.grid(axis='y',linestyle='--')
plt.show()
